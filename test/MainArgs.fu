public static class Runner
{
	public static int Main(string[] args) //FAIL: cl
	{
		if (args.Length == 2 && args[0] == "foo" && args[1] == "bar") {
			int i = 0;
			foreach (string arg in args) {
				if (arg != (i == 0 ? "foo" : "bar"))
					break;
				i++;
			}
			if (i == 2) {
				Console.WriteLine("PASSED");
				return 0;
			}
		}
		Console.WriteLine("FAILED");
		return 1;
	}
}

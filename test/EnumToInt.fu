enum Number
{
	One = 1,
	Five = 5
}

public static class Test
{
	public static bool Run()
	{
		Number one = Number.One;
		int[Number.One.ToInt()] a;
		a[0] = 42;
		return one.ToInt() == 1 && Number.Five.ToInt() == 5 && a[0] == 42;
	}
}

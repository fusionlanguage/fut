public static class Test
{
	public static bool Run()
	{
		double a = 1.75; //FAIL: cl if no double support
		double b = -3.25;
		return Math.Truncate(a) == 1 && Math.Truncate(b) == -3; //FAIL: java
	}
}

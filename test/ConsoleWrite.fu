public static class Test
{
	public static bool Run()
	{
		Console.Write("Hello");
		int i = 42;
		Console.Write(i);
		float f = 10.5;
		Console.Write(f);
		Console.Write($"i={i:X} f={f:e}={f:F}={f}$"); //FAIL: cl if no double support
		return true;
	}
}

public static class Test
{
	public static bool Run()
	{
		int s = 0;
		for (int i = 3; i > 1; i--)
			s += i;
	
		int t = 0;
		for (int i = 3; i >= 1; i--)
			t += i;

		return s == 5 && t == 6;
	}
}

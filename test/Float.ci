public static class Test
{
	public static bool Run()
	{
		float e = Math.E;
		float f = Math.PI;
		float sum = e + f;
		float q = 2.0 / 3;
		return e > 2.71 && e < 2.72 //FAIL: cl TODO if no double support
			&& f > 3.14 && f < 3.15
			&& sum > 5.85 && sum < 5.86
			&& q > 0.66 && q < 0.67;
	}
}

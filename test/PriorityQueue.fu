public static class Test
{
	public static bool Run()
	{
		PriorityQueue<string, int>() q; //FAIL: d js swift ts TODO; c, cl
		q.Enqueue("forty two", 42);
		q.Clear();
		q.Enqueue("fifteen", 15);
		q.Enqueue("five", 5);
		q.Enqueue("one", 1);
		q.Enqueue("ten", 10);
		q.Dequeue();
		PriorityQueue<byte, int>() qb;
		qb.Enqueue(200, 5);
		int b = 222;
		qb.Enqueue(b, 7); //FAIL: cs TODO
		return q.Count == 3 && q.Dequeue() == "five" && q.Peek() == "ten"
			&& qb.Dequeue() == 200 && qb.Peek() == 222;
	}
}

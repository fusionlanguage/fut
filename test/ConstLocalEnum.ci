public enum DayOfWeek
{
	Sunday,
	Monday,
	Tuesday,
	Wednesday,
	Thursday,
	Friday,
	Saturday
}

public static class Test
{
	public static bool Run()
	{
		const DayOfWeek worst = DayOfWeek.Monday;
		return worst == DayOfWeek.Monday;
	}
}

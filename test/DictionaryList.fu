public static class Test
{
	public static bool Run()
	{
		Dictionary<string, List<string>()>() dict; //FAIL: cl
		dict.Add("zamek");
		dict["zamek"].Add("castle");
		dict["zamek"].Add("lock");

		List<int>() list;
		Dictionary<string, int>() dint;
		dint["dwa"] = 2;
		list.Add(dint["dwa"]);

		return dict.Count == 1 && dict["zamek"][0] == "castle" && dict["zamek"][1] == "lock";
	}
}

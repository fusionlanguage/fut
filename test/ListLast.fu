public static class Test
{
	public static bool Run()
	{
		List<string()>() list; //FAIL: cl
		list.Add("foo");
		list.Add("bar");
		list.Add("quux");
		string s = list.Last();
		List<List<int>()>() lol;
		lol.Add();
		lol[0].Add(42);
		int i = lol.Last()[0];
		return list.Last() == "quux" && s == "quux" && i == 42;
	}
}

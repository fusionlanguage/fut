public static class Test
{
	public static bool Run()
	{
		int s = 0;
		for (int i = 0; i < 5; i++)
			s += i;

		int t = 0;
		for (int i = 0; i <= 5; i++)
			t += i;

		return s == 10 && t == 15;
	}
}

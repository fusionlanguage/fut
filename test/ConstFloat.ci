public static class Test
{
	public const float Foo = -10.5;
	public const float[] Array = { -1.0, -2 }; //FAIL: cs java TODO

	public static bool Run()
	{
		float f = Foo;
		return f == -10.5 && Array[1] == -2;
	}
}

public static class Test
{
	public static bool Run()
	{
		double a = 4; //FAIL: cl if no double support
		float f = 9;
		f = Math.Sqrt(f);
		int i = 16;
		return Math.Sqrt(a) == 2 && f == 3 && Math.Sqrt(i) == 4;
	}
}

public static class Test
{
	public static bool Run()
	{
		int[5] a;
		for (int i = 0; i < 5; i++)
			a[i] = i;
		a.Fill(0);
		for (int i = 0; i < 5; i++)
			if (a[i] != 0)
				return false;
		return true;
	}
}

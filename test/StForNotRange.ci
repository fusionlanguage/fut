public static class Test
{
	public static bool Run()
	{
		int s = 0;
		for (int i = 0; i < 5; i++)
			s += ++i;
		int n = 2;
		for (int i = 0; i < n; i++) {
			s += i;
			if (i == 1)
				n++;
		}
		return s == 9 + 3;
	}
}

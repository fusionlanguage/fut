public static class Test
{
	public static bool Run()
	{
		string s = "bar";
		switch (s) {
		case "foo":
		case "quux":
			return false;
		case "bar":
			if (s[0] == 'X')
				break;
			return true;
		default:
			return false;
		}
		return false;
	}
}

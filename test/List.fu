public static class Test
{
	public static bool Run()
	{
		List<int>() list; //FAIL: cl
		list.Add(42);
		list.Clear();
		list.Add(Math.Ceiling(1.0));
		list.Add(42);
		list.Add(3);
		list.RemoveAt(1);
		int two = 2;
		list.Insert(1, two);
		list[0] = 5;
		int[4] a;
		list.CopyTo(1, a, 0, 2);
		list.CopyTo(1, a, 2, 2);
		byte[1] b;
		b[0] = 200;
		list.Add(b[0]);
		list.RemoveRange(1, 1);
		if (list.Count == 100)
			list.Add(100);

		List<float>() lf;
		lf.Add(10.5);
		lf.Add(10.5);
		List<double>() ld;
		double d = -0.25;
		ld.Add(d);
		ld.Add(10.5);

		List<long>() ll;
		ll.Add(Math.Floor(10.3)); //FAIL: js ts TODO

		List<string()>() ls;
		ls.Add("foo");
		string() str = "bar";
		ls.Add(str);
		string sp = "quux";
		ls.Add(sp);
		ls.Insert(0, sp);
		ls.Insert(0, str);
		ls.Insert(0, "foo");

		return list.Count == 3 && list[0] == 5 && list[1] == 3 && list[2] == 200
			&& a[0] == 2 && a[1] == 3 && a[2] == 2 && a[3] == 3
			&& lf.Count == 2 && lf[0] == 10.5 && lf[0] == lf[1]
			&& ld.Count == 2 && ld[0] == -0.25 && lf[0] == ld[1] //FAIL: swift TODO
			&& ll.Count == 1 && ll[0] == 10
			&& ls.Count == 6 && ls[0] == "foo" && ls[1] == "bar" && ls[2] == "quux" && ls[3] == "foo" && ls[4] == "bar" && ls[5] == "quux";
	}
}

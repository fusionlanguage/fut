public static class Test
{
	public static bool Run()
	{
		double a = 0; //FAIL: cl if no double support
		float f = 0;
		f = Math.Sin(f);
		int i = 0;
		return Math.Sin(a) == 0 && f == 0 && Math.Sin(i) == 0; //FAIL: swift TODO
	}
}

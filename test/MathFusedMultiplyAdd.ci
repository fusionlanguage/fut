public static class Test
{
	public static bool Run()
	{
		double x = 2;
		double y = 3;
		double z = 1;
		return Math.FusedMultiplyAdd(x, y, z) == 7; //FAIL: cs - .NET Core but not .NET Framework 4.8; py - install pyfma on Travis
	}
}

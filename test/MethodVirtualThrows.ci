abstract class Base
{
	internal virtual void Foo() throws
	{
	}

	int Bar() throws
	{
		Foo();
		return 42;
	}
}

class Derived : Base
{
	internal override void Foo() throws
	{
	}
}

public static class Test
{
	public static bool Run()
	{
		Derived() d;
		Base p = d;
		return p.Bar() == 42;
	}
}

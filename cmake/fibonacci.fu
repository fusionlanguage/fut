public class Fibonacci
{
	public static int Fib(int n)
	{
		if (n == 0) return 0;
		int prev = 1;
		int curr = 1;
		for (int i = 2; i < n; i++) {
			int fib = prev + curr;
			prev = curr;
			curr = fib;
		}
		return curr;
	}
}

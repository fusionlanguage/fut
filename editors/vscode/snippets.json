{
	"do...while loop": {
		"prefix": "do",
		"body": [
			"do {",
			"\t${TM_SELECTED_TEXT}$0",
			"} while (${1:true});"
		],
		"description": "do...while loop"
	},
	"for loop": {
		"prefix": "for",
		"body": [
			"for (int ${1:i} = 0; ${1:i} < ${2:n}; ${1:i}++) {",
			"\t${TM_SELECTED_TEXT}$0",
			"}"
		],
		"description": "for loop"
	},
	"Reverse for loop": {
		"prefix": "forr",
		"body": [
			"for (int ${1:i} = ${2:n}; --${1:i} >= 0;) {",
			"\t${TM_SELECTED_TEXT}$0",
			"}"
		],
		"description": "Reverse for loop"
	},
	"foreach loop": {
		"prefix": "foreach",
		"body": [
			"foreach (${1:Type item} in ${2:collection}) {",
			"\t${TM_SELECTED_TEXT}$0",
			"}"
		],
		"description": "Foreach statement"
	},
	"resource": {
		"prefix": "resource",
		"body": [
			"resource<byte[]>(\"${1:file.bin}\")"
		],
		"desciption": "binary resource"
	},
}
